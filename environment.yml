name: openbis2sqlite
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - ca-certificates
  - openssl
  - pytest
  - pip:
    - pgdumplib
prefix: /home/ckujath/miniconda3/envs/openbis2sqlite
